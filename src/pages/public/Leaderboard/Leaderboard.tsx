import { Page } from "../../../components/layout";

const Leaderboard = () => {
    return (
        <Page title="Leaderboard">
            <h1>Leaderboard</h1>
        </Page>
    );
};

export default Leaderboard;